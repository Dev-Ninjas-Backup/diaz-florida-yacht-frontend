services:
  floridayt:
    platform: linux/amd64
    image: softvence/diaz_floridayt:latest
    container_name: floridayt
    environment:
      - NEXT_PUBLIC_BASE_URL=https://development.floridayachttrader.com
      - NEXT_PUBLIC_BASE_API=https://api.floridayachttrader.com/api
      - NEXT_PUBLIC_CHATBOT_API_URL=https://ai.jupitermarinesales.com/api/v1
      - NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_51RlFNRQNJ9V9C9o5Rz8lIUhKRJ7foy4WRfQR4z05Oy41puNBooAmZE0KuBy2K55iGVHrioX1CY3KotwYxwIyxHR800J4o3qINh
    ports:
      - "3000:3000"
    restart: always
    command: >
      sh -c "
        echo '🚀 Starting app on 0.0.0.0...';
        HOST=0.0.0.0 PORT=3000 npm run start
      "
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://0.0.0.0:3000']
      interval: 30s
      timeout: 10s
      retries: 5

  caddy:
    image: caddy
    container_name: floridayt_caddy
    restart: always
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - /var/www/html:/var/www/html
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      floridayt:
        condition: service_healthy
    healthcheck:
      test: ['CMD-SHELL', 'caddy validate --config /etc/caddy/Caddyfile']
      interval: 30s
      timeout: 5s
      retries: 3

volumes:
  caddy_data:
  caddy_config:
